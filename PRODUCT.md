# Product

## Platform

web

（Tauri v2 桌面壳 + React/Vite 前端，发行于 macOS 与 Windows。渲染层是 Web，
设计语言按桌面应用而非移动原生对待。窗口 1050×700，最小 920×620，可缩放；
顶栏导航。macOS 隐藏标题栏、红绿灯垂直居中悬浮在顶栏左侧，Windows 用标准标题栏。）

## Users

**主要用户：从抖音来的非技术用户**（确认自作者本人）。

他们的处境：跟着「Winhao学AI」的视频或图文教程走，手上有一个第三方模型的
API 密钥，想让已经装好的 Claude Desktop 用上这个模型。他们不写代码，不知道
什么是「反向代理」「槽位」「推理强度」「上下文窗口」，也不打算知道。

他们要完成的事，按发生频率排：

1. **第一次配置**（几乎所有人只经历一次）：挑服务商 → 粘贴密钥 → 点「应用到
   Claude Desktop」→ 回 Claude 里选模型开始聊。这条路径走通，产品的价值就
   100% 交付了。
2. **出问题时来排查**：Claude 里对话报错、模型不见了、突然变贵了。这时他们
   打开 ModelLink，需要在几秒内看出「是不是我这边的问题、该点哪里」。
3. **偶尔加一个模型 / 换一家服务商**：上游出了新模型，或者换了更便宜的一家。

次要用户：开发者与 AI 重度用户（看日志、调推理强度、比费率）。他们不是设计
的主要对象，但产品里已有的高级信息不应为了照顾主要用户而被删掉——收起来、
分层，不要丢失。

## Product Purpose

把国产大模型接进 Claude Desktop，让用户继续用 Claude 的客户端界面，底下换成
自己付得起的模型。

ModelLink 是一个跑在 `127.0.0.1:5678` 的本地代理：它把 Claude Desktop 发来的
Anthropic 格式请求转发给第三方 OpenAI/Anthropic 兼容端点，并且**直接改写
Claude Desktop 的第三方推理配置文件**，让用户不必手动填任何 JSON。

成功的定义：用户点完一次「应用到 Claude Desktop」，Claude 自动重启，模型选择器
里出现他配的模型真名（例如 `Kimi-k2.6`），选中就能聊。全程不需要理解中间发生
了什么。

## Positioning

同类工具的做法是让用户自己去 Claude Desktop 的开发者菜单里填 Gateway URL 和一
大段 JSON。ModelLink 的区别在于**它直接写 Claude Desktop 的配置**——安装、填
密钥、点一个按钮，Claude 重启后就好了。

由此派生出两件邻近产品无法照抄的事：

- **模型链路板**：Claude 里的模型槽位 → 用户真实模型的映射被画了出来。因为
  ModelLink 是唯一知道这张映射表的一方。
- **跟随 Claude Desktop 的能力**：推理强度选择器、真实费率表、1M 上下文、
  Chat 页——这些都是 Claude Desktop 自己的配置键，ModelLink 逐个核实 app.asar
  里的 schema 后写进去。用户在 Claude 里拿到的是原生能力，不是模拟出来的。

## Operating Context

- **ModelLink 不是用户停留的地方**。用户真正干活的界面是 Claude Desktop。
  ModelLink 是一个配置器：打开、配好、关掉，然后长期在托盘/菜单栏里静默运行。
  界面设计必须服务于「快速进出」，不是「久留」。
- **应用前后是两个世界**。改了配置但没点「应用」，Claude 那边什么都没变。这个
  状态差是产品里最重要的一条信息。
- **窗口关掉 ≠ 退出**。代理继续在托盘里运行，Claude 才能继续工作。
- 用户的另一半时间在抖音评论区或私信里问作者「为什么不行」。界面上任何一句话
  都可能被截图发过来，所以自解释程度决定作者的售后成本。
- macOS 与 Windows 双平台；Windows 首次使用还需在 Claude Desktop 里手动填一次
  Gateway URL 和 API Key（ModelLink 写不了那一步）。

## Capabilities and Constraints

**当前能力（2.1）**

- 多服务商并存，模型总数上限 **20**（`MAX_MODELS`，后端 config.rs 镜像）
- 内置 8 家预设：DeepSeek / Kimi Code / Kimi 开放平台 / MiniMax / 百炼 Coding /
  百炼 Token / GLM（智谱）/ mimo，带品牌图标；也可自定义
- **槽位池**：前 8 个模型占 `claude-opus-5` 等真实 Claude 模型 ID，其中前 4 个
  在 Claude 里有 5 档推理强度，第 5–6 个有 4 档，第 7–8 个没有选择器；第 9 个
  以后走 `claude-ml-N` 溢出层（也没有选择器）。**槽位顺序 = 能力差异**，这是
  目前界面里最难解释也最必须解释的一件事。
- 真实费率表（从 models.dev 同步，最多 6 小时一次，可关；手填的不覆盖）
- 1M 上下文变体，按模型真实 `context_limit` 判定，装不下时界面标出真实上限
- 上游报错自动整流重试（不认 effort / thinking 预算过小 / 思考块签名验不过）
- 长生成 SSE 保活心跳；连接测试；请求日志（最近 100 条，2 秒轮询）
- 自动保存（400ms 防抖），无保存按钮；应用状态机 clean/dirty/applying/error
- 检测 Claude Desktop 版本，版本过低的能力在设置页点名
- 开机自启、代理端口可改（1024–65535，热切换）、亮/暗/跟随系统、应用内更新

**约束**

- 中文单语，无 i18n 需求
- 桌面窗口，非响应式网页；不需要移动端适配
- 无账号、无云端、无遥测；除 models.dev 费率同步与检查更新外不主动联网
- 免费软件，CC BY-NC-ND 4.0，禁止商业化

**用户已明确放开的历史约束（2026-09-14）**

- 侧栏防篡改水印**可以去掉**
- 品牌陶土色 `#D97757` **可以换**
- `docs/design.md` §13 的逐字文案表**不再是硬约束**
- 880×640 窗口与 178px 侧栏骨架**可以改**

## Brand Commitments

- 产品名 **ModelLink**，作者署名 **Winhao学AI**（抖音同名，抖音号 54927876676）
- 「完全免费 · 仅供个人学习和非商业用途 · 严禁商业化」必须在产品内可见——
  具体呈现形式不限（原先是侧栏常驻水印，用户已同意改）
- 语气：直白、口语、不端着。README 里「如果你是花钱买到的这个软件，你被骗了，
  请举报卖家」这句就是产品的声音。
- 现有资产：服务商品牌单色 SVG（MIT 许可，来源见 `gui-tauri/src/assets/providers/README.md`）、Archivo 与
  Spline Sans Mono 字体子集（OFL，本地打包，不走 CDN）、应用图标。

## Evidence on Hand

- 真实截图：`docs/images/overview.png`、`guide.png`、`providers.png`
- 现有视觉规格：`docs/design.md`（2.0 冻结版，2.1 已跑偏——槽位写 8 实际 20，
  未覆盖费率同步、Claude Desktop 版本检测、整流标记、推理强度档位徽章）
- 2.1 设计依据与实测修订：`docs/design-2.1-follow-desktop.md`
- 工程约束与历史教训：`CLAUDE.md`
- **没有的东西，后续工作不得编造**：用户数、下载量、评测数据、客户案例、
  媒体报道、定价（产品免费）、任何第三方背书。

## Product Principles

1. **不制造静默错误。** 用户永远不该「以为 A、实际是 B 且无从察觉」。宁可报错、
   宁可标出真实上限、宁可不显示费用，也不给假账单。这条已经杀掉过一个功能
   （健康检查本地应答）和一个默认行为（费率缺失时按 Anthropic 官价估算）。
2. **应用状态差是第一信息。** 「改了没有」和「生效了没有」是两件事，必须在任何
   页面都看得见，且指向同一个动作。
3. **说明文字写给用户，不写给开发者。** 不暴露内部机制。用户需要知道的是
   「开着更省钱」，不是「实测花掉 88 个思考 token」。
4. **控件的去留看用户的决策依据。** 「永远不需要动」或「得懂 ModelLink 内部机制
   才能决定」的，不做成设置项。2.1 据此把设置页从 13 个控件砍到 6 个。
5. **ModelLink 是过路的，不是目的地。** 优化进出速度，不优化停留时长。
